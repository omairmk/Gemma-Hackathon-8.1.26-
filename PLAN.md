# GI Hackathon Execution Plan

Updated: 2026-07-31 12:42 EDT

- Track A — BLOCKED ON OPERATOR/UPSTREAM: starter app, exact remote v0.14.0 pin, 14 passing host tests, 11 passing arm64 app-target simulator tests in a checksum-corrected temporary overlay, model-readiness/import-race gating, real SwiftData rollback fault coverage, fixtures, and blank physical gates are present. The frozen exact remote pin is blocked by the upstream v0.14.0 checksum mismatch; starter-code permission and every physical-phone gate remain operator-only.
- Track B — HONEST NO-GO, AUDITED: both pinned local snapshots, workspace Python/runtime, schema, stool-like watermarked fixtures, before/after executable/PID/socket and snapshot-content binding, evidence/candidate revalidation, fail-closed route handling, verified Wi-Fi restoration, and 33 passing tests are present. Both model-server attempts stopped before binding because the sandbox exposes no Metal device; no inference/socket/eligibility/offline result is claimed. The physical-Mac continuation must fully qualify E4B and then E2B separately on port 8080, select eligible E4B only at validated warm latency ≤60 seconds and otherwise independently eligible E2B, then run offline proof only for that selected candidate.
- Track C — ASSETS DONE; PUBLICATION BLOCKED: event drafts, exact private operator guides, claim controls, and hygiene checks are present; exact organizer form links were grounded from Gmail. Do not publish until the owner selects a repository license, third-party/model notices are verified, and private form links/local identifiers are removed from a public copy.
- Integration — FINALIZING HANDOFF: source recovery and agent claims have been independently checked; Track A is committed at `ef191d4` and Track B's frozen `NO-GO` at `3056ea5`. The remaining work is the integration-document commit, final hygiene verification, and private packaged handoff; every runtime/phone/form/rules task after that requires the operator or an upstream dependency correction.
